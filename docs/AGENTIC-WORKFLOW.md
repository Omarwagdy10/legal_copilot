# Agentic Workflow
- Use the repository instructions and prompt files before editing business logic.
- Keep agents specialized: extraction, risk, memo.
- Ask a security-review pass before exposing new tools.
- Keep prompt assets versioned under `backend/prompts/`.
- Validate outputs against expected JSON shapes.

Deferred: automated hooks and MCP server.
